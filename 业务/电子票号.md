## 电子票号查询

取电子发票三个步骤：

1. 申请开具

js 爬取文件目录所有文件及文件信息，并生成sql insert语句，sql 有main_id 主键（32位且是自增的，不要从零开始）、file_name 文件名、file_desc 为文件描述为视频名，create_time为创建时间，ip_path为文件路径，file_size为文件大小（字节数），play_duration为视频时长，flag1、flag2、flag3为null